public class Recursion{
  
  public static double squareRoot(x, guess, error){
    error = x - guess * guess;
    if (error <= acceptedError)
      return guess;
    else {
      newGuess = 0.5 * (lastGuess + x / lastGuess);
      squareRoot(x, newGuess, error);
    }
  }
    
  
  
  
  
  
}